/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['ko-KR'],
    defaultLocale: 'ko-KR',
    localeDetection: false,
  },
  reactStrictMode: true,
};

export default nextConfig;
